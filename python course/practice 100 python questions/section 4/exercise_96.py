# create a program that asks the user to submit text repeatedly the program closes when the user submits CLOSE

while True:
    user_input = input("Enter a word : ")
    while user_input != "CLOSE":
        break